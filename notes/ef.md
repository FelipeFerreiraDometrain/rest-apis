in ctx of `src` directory we run the following commands

## Adding migrations
dotnet ef migrations add InitialCreate --project adapters/Dometrain.Movies.InMemoryDataStore --startup-project apps/Dometrain.Movies.WebService